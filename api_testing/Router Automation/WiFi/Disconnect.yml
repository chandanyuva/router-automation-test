info:
    name: Disconnect
    type: http
    seq: 3

http:
    method: POST
    url: "{{baseUrl}}/wifi/disconnect"
    body:
        type: none
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
