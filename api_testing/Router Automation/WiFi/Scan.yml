info:
  name: Scan Networks
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/wifi/scan"
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5