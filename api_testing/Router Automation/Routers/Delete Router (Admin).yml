info:
  name: Delete Router (Admin)
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/routers/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
